/* Spliced verbatim. Its @PACKAGE_VERSION@ must NOT be substituted: gnulib's
   `r` commands run in the recipe's LAST sed pass, after every substitution,
   so a spliced file's own @VAR@ references survive into the header. */
#define HELPER_MACRO(x) ((x) + 1)
