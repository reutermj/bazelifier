#include <stdio.h>

/* Registered with CTest as `app_test`, whose command IS this built
 * executable — the direction that must translate to an sh_test rather than
 * escalate. */
int main(void) {
    printf("app ok\n");
    return 0;
}
