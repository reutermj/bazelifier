/* Spliced verbatim by sed's `r`. Deliberately contains @PACKAGE_VERSION@,
   which must NOT be substituted: gnulib runs the `r` commands in the last
   sed pass, after every substitution. */
#define HELPER_ADD(x) ((x) + 41)
